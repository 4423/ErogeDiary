<UserControl x:Class="ErogeDiary.Views.Contents.PlayLogs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:controls="clr-namespace:ErogeDiary.Controls.CalendarHeatmap;assembly=ErogeDiary.Controls"
             xmlns:local="clr-namespace:ErogeDiary.Views.Contents" 
             xmlns:properties="clr-namespace:ErogeDiary.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <ComboBox ItemsSource="{Binding PlayLogDateRanges}" DisplayMemberPath="Label"
                      SelectedItem="{Binding SelectedPlayLogDateRange}"/>
            <TextBlock Text="{x:Static properties:Strings.PlayLogs_PlayHistorySuffix}" VerticalAlignment="Center" Margin="10 0 0 0" />
        </StackPanel>

        <controls:CalendarHeatmap ChartData="{Binding HeatmapData}"
                                  CellBrushSelector="{Binding CellBrushSelector}"
                                  CellToolTipSelector="{Binding CellToolTipSelector}"
                                  Margin="0 10 0 15"/>
        
        <StackPanel>
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="FontSize" Value="11"/>
                    <Setter Property="Foreground" Value="{DynamicResource PlayLogSummaryForegroundBrush}"/>
                </Style>
            </StackPanel.Resources>
            <StackPanel Orientation="Horizontal" Visibility="{Binding FirstPlayedAt}">
                <TextBlock Text="{x:Static properties:Strings.PlayLogs_FirstPlayed}"/>
                <TextBlock Text="{Binding FirstPlayedAt, StringFormat={x:Static properties:Strings.DateFormat_Long}}" Margin="7 0 0 0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Visibility="{Binding Game.LastPlayedAt}">
                <TextBlock Text="{x:Static properties:Strings.PlayLogs_LastPlayed}"/>
                <TextBlock Text="{Binding Game.LastPlayedAt, StringFormat={x:Static properties:Strings.DateFormat_Long}}" Margin="7 0 0 0"/>
            </StackPanel>
        </StackPanel>

        <local:PlayLogHistogram DataContext="{Binding PlayLogHistogram}" Margin="0 30 0 0"/>
    </StackPanel>
</UserControl>
